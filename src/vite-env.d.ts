/// <reference types="vite/client" />

/** Injected by vite.config.ts. Shown in the header so a stale cached build is
 *  identifiable rather than merely suspected. */
declare const __BUILD_ID__: string
